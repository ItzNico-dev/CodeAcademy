class Person {
	constructor(name, age) {
		this.name = name;
		this.age = age;
	}

	compareAge(other) {
		// Rašyti kodą čia
	}
}

const p1 = new Person("Petras", 16)
p1.compareAge() => "Petras is old enough to drink";